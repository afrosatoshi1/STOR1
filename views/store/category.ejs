<!DOCTYPE html><html><head>
<meta charset="utf-8"/><meta name="viewport" content="width=device-width, initial-scale=1"/>
<link rel="stylesheet" href="/css/styles.css"/>
<title><%= category.name %> - NeoTech</title></head><body>
<%- include('../partials/header', { user }) %>
<h1 class="hero"><%= category.name %></h1>
<div class="grid">
  <% products.forEach(p => { %>
    <div class="card">
      <img src="<%= p.image || '/img/placeholder.png' %>" alt="<%= p.name %>"/>
      <h3><%= p.name %></h3>
      <p class="muted"><%= p.description %></p>
      <div class="price">₦<%= p.price.toLocaleString() %></div>
      <form method="POST" action="/cart/add" style="margin-top:8px">
        <input type="hidden" name="product_id" value="<%= p.id %>"/>
        <div class="row3">
          <a class="btn" href="/product/<%= p.id %>">View</a>
          <input type="number" name="qty" value="1" min="1"/>
          <button class="btn" type="submit">Add</button>
        </div>
      </form>
    </div>
  <% }) %>
</div>
</body></html>
